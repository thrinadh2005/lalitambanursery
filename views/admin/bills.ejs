<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/ui-fixes.css">

    <!-- Theme Initialization -->
    <script>
        (function() {
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme) {
                document.documentElement.setAttribute('data-theme', savedTheme);
            } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
                document.documentElement.setAttribute('data-theme', 'dark');
            }
        })();
    </script>

    <style>
        .admin-page-header {
            background: var(--gradient-primary);
            color: white;
            padding: 4rem 0;
            margin-bottom: 2rem;
            border-radius: 0 0 var(--radius-3xl) var(--radius-3xl);
            box-shadow: var(--shadow-lg);
            position: relative;
            overflow: hidden;
        }

        .admin-page-header::after {
            content: '';
            position: absolute;
            inset: 0;
            background: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 86c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zm66-3c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zm-40-39c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zm43 38c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='%23ffffff' fill-opacity='0.05'/%3E%3C/svg%3E");
            opacity: 0.1;
        }

        .table-glass-card {
            background: var(--glass-bg);
            backdrop-filter: blur(var(--glass-blur));
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-2xl);
            overflow: hidden;
            box-shadow: var(--shadow-xl);
        }

        .bill-number-code {
            font-family: 'Inter', monospace;
            font-weight: 700;
            color: var(--primary);
            background: var(--primary-light);
            padding: 0.25rem 0.5rem;
            border-radius: 6px;
            font-size: 0.85rem;
        }

        .btn-action-icon {
            width: 36px;
            height: 36px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
            background: var(--surface);
            border: 1px solid var(--border);
            color: var(--text-muted);
            transition: all 0.2s ease;
            text-decoration: none;
        }

        .btn-action-icon:hover {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
            transform: translateY(-2px);
        }

        .status-badge {
            font-weight: 700;
            font-size: 0.75rem;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
    </style>
</head>

<body class="bg-light">
    <%- include('partials/navbar') %>

    <div class="admin-page-header">
        <div class="container px-5">
            <div class="row align-items-center">
                <div class="col-md-7" data-aos="fade-right">
                    <h1 class="display-5 fw-900 mb-2">Financial Records</h1>
                    <p class="mb-0 opacity-75 fs-5">Monitor invoices, track payments, and audit business revenue</p>
                </div>
                <div class="col-md-5 text-md-end mt-4 mt-md-0" data-aos="fade-left">
                    <div class="d-flex gap-3 justify-content-md-end align-items-center">
                        <div class="dropdown">
                            <button class="btn btn-light rounded-pill px-4 shadow-sm dropdown-toggle fw-bold" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-filter me-2 text-primary"></i>Filter
                            </button>
                            <ul class="dropdown-menu border-0 shadow-lg rounded-4">
                                <li><a class="dropdown-item py-2 px-4" href="/admin/bills?status=all">All Invoices</a></li>
                                <li><a class="dropdown-item py-2 px-4 text-success" href="/admin/bills?status=paid">Paid Only</a></li>
                                <li><a class="dropdown-item py-2 px-4 text-warning" href="/admin/bills?status=pending">Pending</a></li>
                                <li><a class="dropdown-item py-2 px-4 text-danger" href="/admin/bills?status=overdue">Overdue</a></li>
                            </ul>
                        </div>
                        <a href="/admin/bills/create" class="btn btn-warning rounded-pill px-4 py-2 fw-bold shadow-sm">
                            <i class="fas fa-plus-circle me-2"></i>Create Invoice
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid px-5 py-4">
        <div class="row g-4">
            <main class="col-12">
                <!-- Quick Stats -->
                <% if (typeof bills !== 'undefined' && bills.length > 0) { %>
                    <div class="row g-4 mb-5" data-aos="fade-up">
                        <div class="col-md-4">
                            <div class="glass p-4 rounded-4 shadow-sm h-100 border-start border-4 border-success">
                                <div class="text-muted small fw-bold text-uppercase mb-1">Total Revenue</div>
                                <% const totalRevenue = bills.reduce((sum, bill) => sum + bill.totalAmount, 0); %>
                                <div class="h3 fw-900 text-dark mb-0">₹<%= totalRevenue.toLocaleString('en-IN') %></div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="glass p-4 rounded-4 shadow-sm h-100 border-start border-4 border-warning">
                                <div class="text-muted small fw-bold text-uppercase mb-1">Pending Receivables</div>
                                <% const pendingAmt = bills.filter(b => b.status !== 'paid').reduce((sum, b) => sum + (b.balanceAmount || (b.totalAmount - (b.paidAmount || 0))), 0); %>
                                <div class="h3 fw-900 text-warning mb-0">₹<%= pendingAmt.toLocaleString('en-IN') %></div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="glass p-4 rounded-4 shadow-sm h-100 border-start border-4 border-primary">
                                <div class="text-muted small fw-bold text-uppercase mb-1">Active Invoices</div>
                                <div class="h3 fw-900 text-primary mb-0"><%= bills.length %></div>
                            </div>
                        </div>
                    </div>
                <% } %>

                <div class="table-glass-card" data-aos="fade-up">
                    <div class="p-4 border-bottom bg-white bg-opacity-50 d-flex justify-content-between align-items-center">
                        <h5 class="mb-0 fw-bold"><i class="fas fa-file-invoice-dollar me-2 text-primary"></i>Audit Log</h5>
                        <div class="badge bg-primary bg-opacity-10 text-primary rounded-pill px-3 py-2">Fiscal Year 2026</div>
                    </div>

                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead>
                                <tr>
                                    <th class="ps-4">Invoice</th>
                                    <th>Recipient</th>
                                    <th>Date Issued</th>
                                    <th>Valuation</th>
                                    <th>Outstanding</th>
                                    <th>Status</th>
                                    <th class="text-end pe-4">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (typeof bills !== 'undefined' && bills.length > 0) { %>
                                    <% bills.forEach(bill => { %>
                                        <tr>
                                            <td class="ps-4">
                                                <div class="bill-number-code mb-1">#<%= bill.billNumber %></div>
                                                <div class="text-muted extra-small">ID: <%= bill._id.toString().slice(-6).toUpperCase() %></div>
                                            </td>
                                            <td>
                                                <div class="fw-bold text-dark"><%= bill.customerName %></div>
                                                <% if (bill.isFarmerBill) { %>
                                                    <span class="badge bg-success bg-opacity-10 text-success extra-small">Farmer Account</span>
                                                <% } %>
                                            </td>
                                            <td>
                                                <div class="fw-semibold text-dark"><%= new Date(bill.billDate).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' }) %></div>
                                                <div class="text-muted extra-small"><%= new Date(bill.billDate).getFullYear() %></div>
                                            </td>
                                            <td class="fw-800 text-dark">₹<%= bill.totalAmount.toLocaleString('en-IN') %></td>
                                            <td class="text-danger fw-bold">₹<%= (bill.balanceAmount || (bill.totalAmount - (bill.paidAmount || 0))).toLocaleString('en-IN') %></td>
                                            <td>
                                                <% 
                                                    let statusClass = 'secondary';
                                                    if (bill.status === 'paid') statusClass = 'success';
                                                    else if (bill.status === 'partially_paid') statusClass = 'warning';
                                                    else if (bill.status === 'pending') statusClass = 'info';
                                                    else if (bill.status === 'overdue') statusClass = 'danger';
                                                %>
                                                <span class="status-badge bg-<%= statusClass %> bg-opacity-10 text-<%= statusClass %> border border-<%= statusClass %> border-opacity-25">
                                                    <%= bill.status.replace('_', ' ') %>
                                                </span>
                                            </td>
                                            <td class="text-end pe-4">
                                                <div class="d-flex justify-content-end gap-2">
                                                    <button class="btn-action-icon" title="Record Payment" onclick="openPaymentModal('<%= bill._id %>', '<%= bill.customerName %>', <%= bill.balanceAmount || (bill.totalAmount - (bill.paidAmount || 0)) %>)">
                                                        <i class="fas fa-hand-holding-dollar"></i>
                                                    </button>
                                                    <a href="/admin/bills/<%= bill._id %>" class="btn-action-icon" title="View Ledger">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    <a href="/admin/bills/<%= bill._id %>/print" target="_blank" class="btn-action-icon" title="Print Invoice">
                                                        <i class="fas fa-print"></i>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="7" class="text-center py-5">
                                            <div class="text-muted py-5">
                                                <i class="fas fa-file-invoice-dollar fa-4x mb-3 opacity-25"></i>
                                                <h5 class="fw-bold">No Invoices Found</h5>
                                                <p>Issue your first customer bill to start tracking revenue.</p>
                                                <a href="/admin/bills/create" class="btn btn-primary rounded-pill px-4">Generate Bill</a>
                                            </div>
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Bill Preview Modal -->
    <div class="modal fade" id="billPreviewModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content glass border-0 shadow-2xl rounded-4">
                <div class="modal-header border-bottom bg-white bg-opacity-50 py-3">
                    <h5 class="modal-title fw-900 text-primary" id="previewBillNumber">Invoice Summary</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4" id="billPreviewContent">
                    <div class="text-center py-5">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Syncing...</span>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-top bg-white bg-opacity-50">
                    <button type="button" class="btn btn-outline-secondary rounded-pill px-4" data-bs-dismiss="modal">Close</button>
                    <a href="#" id="previewPrintBtn" target="_blank" class="btn btn-primary rounded-pill px-4">
                        <i class="fas fa-print me-2"></i>Generate PDF
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Payment Modal -->
    <div class="modal fade" id="paymentModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content glass border-0 shadow-2xl rounded-4">
                <div class="modal-header border-bottom bg-white bg-opacity-50 py-3">
                    <h5 class="modal-title fw-900 text-primary" id="paymentModalTitle">Record Transaction</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form id="paymentForm">
                    <input type="hidden" id="paymentBillId" name="billId">
                    <div class="modal-body py-4 px-4">
                        <div class="mb-4 text-center p-3 glass rounded-4 border">
                            <div class="text-muted extra-small fw-800 text-uppercase mb-1">Outstanding Balance</div>
                            <h2 class="fw-900 text-danger mb-0" id="modalBalance">₹0</h2>
                        </div>
                        
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label fw-800 extra-small text-uppercase">Payment Date</label>
                                <input type="date" class="form-control" name="date" id="paymentDate" required>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-800 extra-small text-uppercase">Collection Mode</label>
                                <select class="form-select" name="mode" id="paymentMode" required>
                                    <option value="cash">Cash</option>
                                    <option value="upi">UPI / Digital</option>
                                    <option value="bank_transfer">Bank Transfer</option>
                                    <option value="cheque">Cheque</option>
                                </select>
                            </div>
                            <div class="col-12">
                                <label class="form-label fw-800 extra-small text-uppercase">Transaction Amount (₹)</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0">₹</span>
                                    <input type="number" class="form-control border-start-0 fw-bold fs-5" name="amount" id="paymentAmount" required min="1">
                                </div>
                            </div>
                            <div class="col-12">
                                <label class="form-label fw-800 extra-small text-uppercase">Reference ID</label>
                                <input type="text" class="form-control" name="reference" id="paymentRef" placeholder="TXN-XXXXXX">
                            </div>
                            <div class="col-12">
                                <label class="form-label fw-800 extra-small text-uppercase">Internal Memo</label>
                                <textarea class="form-control" name="notes" id="paymentNotes" rows="2" placeholder="Private admin notes..."></textarea>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer border-top bg-white bg-opacity-50">
                        <button type="button" class="btn btn-outline-secondary rounded-pill px-4" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary rounded-pill px-4 fw-bold">
                            <i class="fas fa-check-circle me-2"></i>Post Payment
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script src="/js/utils.js"></script>
    <script>
        AOS.init({ duration: 800, once: true });

        const paymentModal = new bootstrap.Modal(document.getElementById('paymentModal'));
        const paymentForm = document.getElementById('paymentForm');

        function openPaymentModal(billId, customerName, balance) {
            document.getElementById('paymentBillId').value = billId;
            document.getElementById('paymentModalTitle').innerText = `Payment: ${customerName}`;
            document.getElementById('modalBalance').innerText = `₹${balance.toLocaleString('en-IN')}`;
            document.getElementById('paymentAmount').value = balance;
            document.getElementById('paymentAmount').max = balance;
            
            const now = new Date();
            document.getElementById('paymentDate').value = now.toISOString().split('T')[0];
            
            paymentModal.show();
        }

        paymentForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            const billId = document.getElementById('paymentBillId').value;
            const formData = new FormData(paymentForm);
            const data = Object.fromEntries(formData.entries());

            try {
                const response = await fetch(`/admin/bills/${billId}/payments`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                const result = await response.json();
                if (result.success) {
                    location.reload();
                } else {
                    alert(result.message || 'Error recording payment');
                }
            } catch (error) {
                alert('Network error occurred');
            }
        });
    </script>
</body>

</html>
/script>
</body>

</html>
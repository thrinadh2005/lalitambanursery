<link rel="manifest" href="/manifest.json">
<link rel="icon" type="image/png" href="/images/logo.png">
<link rel="apple-touch-icon" href="/images/logo-192.png">
<script>
    if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
            navigator.serviceWorker.register('/sw.js')
                .then(reg => console.log('Service Worker registered!'))
                .catch(err => console.log('Service Worker registration failed:', err));
        });
    }
</script>
<div class="preloader" id="preloader">
    <div class="loader-content">
        <div class="loader-logo mb-3">
            <img src="/images/logo.png" alt="Sri Lalitamba Logo" style="width: 80px; height: 80px; border-radius: 20px;">
        </div>
        <div class="loader-text">Sri Lalitamba</div>
    </div>
</div>
<div class="scroll-progress-container">
    <div class="scroll-progress-bar" id="scrollProgress"></div>
</div>
<nav class="navbar navbar-expand navbar-light sticky-top py-2 py-md-3">
    <div class="container d-flex flex-nowrap align-items-center justify-content-between">
        <a class="navbar-brand fw-800 d-flex align-items-center me-3" href="/" style="flex-shrink: 0;">
            <img src="/images/logo.png" alt="Logo" class="me-2" style="width: 36px; height: 36px; border-radius: 8px;">
            <div class="brand-text d-none d-sm-flex">
                <span class="brand-subtitle">SRI LALITAMBA</span>
                <span class="brand-title" style="font-size: 1rem;">NURSERY</span>
            </div>
        </a>

        <div class="nav-scroll-container d-flex flex-grow-1 justify-content-end align-items-center" id="navbarNav">
            <ul class="navbar-nav flex-row flex-nowrap align-items-center ms-auto gap-1 gap-md-3 mb-0">
                <li class="nav-item">
                    <a class="nav-link text-nowrap" href="/">
                        <i class="fas fa-home me-1"></i><span class="d-none d-md-inline ms-1">Home</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-nowrap" href="/gallery">
                        <i class="fas fa-images me-1"></i><span class="d-none d-md-inline ms-1">Gallery</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-nowrap" href="/contact">
                        <i class="fas fa-envelope me-1"></i><span class="d-none d-md-inline ms-1">Contact</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-nowrap" href="/about">
                        <i class="fas fa-info-circle me-1"></i><span class="d-none d-md-inline ms-1">About</span>
                    </a>
                </li>

                <!-- Theme Toggle -->
                <li class="nav-item px-lg-2">
                    <button class="btn btn-link nav-link theme-toggle-btn shadow-none" id="themeToggle" title="Toggle Theme">
                        <i class="fas fa-moon theme-icon-dark"></i>
                        <i class="fas fa-sun theme-icon-light d-none"></i>
                    </button>
                </li>

                <% if (user) { %>
                    <li class="nav-item dropdown position-static position-md-relative">
                        <a class="nav-link dropdown-toggle text-nowrap" href="#" id="userDropdown" data-bs-toggle="dropdown" style="display: flex; align-items: center;">
                            <div class="user-avatar" style="width: 24px; height: 24px; margin-right: 0.25rem;">
                                <i class="fas fa-user" style="font-size: 0.7rem;"></i>
                            </div>
                            <span class="user-name d-none d-md-inline">
                                <%= user.firstName %>
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end position-absolute">
                            <li class="dropdown-header">
                                <div class="user-info">
                                    <p class="user-email">
                                        <%= user.email %>
                                    </p>
                                    <p class="user-role">
                                        <%= user.role %>
                                    </p>
                                </div>
                            </li>
                            <li><a class="dropdown-item" href="/profile">
                                    <i class="fas fa-user me-2"></i>My Profile
                                </a></li>
                            <li><a class="dropdown-item" href="/orders">
                                    <i class="fas fa-shopping-bag me-2"></i>My Orders
                                </a></li>
                            <li><a class="dropdown-item" href="/orders/create">
                                    <i class="fas fa-plus-circle me-2"></i>Create Custom Order
                                </a></li>
                            <% if (user.role==='admin' ) { %>
                                <li><a class="dropdown-item" href="/admin/dashboard">
                                        <i class="fas fa-tachometer-alt me-2"></i>Admin Dashboard
                                    </a></li>
                                <li><a class="dropdown-item" href="/admin/plants">
                                        <i class="fas fa-leaf me-2"></i>Manage Plants
                                    </a></li>
                                <% } %>
                                    <li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <li><a class="dropdown-item text-danger" href="/logout">
                                            <i class="fas fa-sign-out-alt me-2"></i>Sign Out
                                        </a></li>
                        </ul>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="btn btn-success px-2 px-md-4 py-1 py-md-2 text-nowrap d-flex align-items-center" href="/login" style="font-size: 0.85rem;">
                                <i class="fas fa-sign-in-alt me-md-2"></i><span class="d-none d-md-inline ms-1">Login</span>
                            </a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>

<!-- Enhanced Global Message Center -->
<div class="message-container">
    <% if (messages && messages.success_msg && messages.success_msg.length> 0) { %>
        <% if (typeof messages.success_msg==='string' ) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <div class="alert-content">
                    <i class="fas fa-check-circle alert-icon"></i>
                    <div class="alert-text">
                        <strong>Success!</strong>
                        <span>
                            <%= messages.success_msg %>
                        </span>
                    </div>
                </div>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } else if (Array.isArray(messages.success_msg)) { %>
                <% messages.success_msg.forEach(function(msg) { %>
                    <% if (msg && msg.length> 0) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <div class="alert-content">
                                <i class="fas fa-check-circle alert-icon"></i>
                                <div class="alert-text">
                                    <strong>Success!</strong>
                                    <span>
                                        <%= msg %>
                                    </span>
                                </div>
                            </div>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                        <% } %>
                            <% }); %>
                                <% } %>
                                    <% } %>

                                        <% if (messages && messages.error_msg && messages.error_msg.length> 0) { %>
                                            <% if (typeof messages.error_msg==='string' ) { %>
                                                <div class="alert alert-danger alert-dismissible fade show"
                                                    role="alert">
                                                    <div class="alert-content">
                                                        <i class="fas fa-exclamation-triangle alert-icon"></i>
                                                        <div class="alert-text">
                                                            <strong>Error!</strong>
                                                            <span>
                                                                <%= messages.error_msg %>
                                                            </span>
                                                        </div>
                                                    </div>
                                                    <button type="button" class="btn-close"
                                                        data-bs-dismiss="alert"></button>
                                                </div>
                                                <% } else if (Array.isArray(messages.error_msg)) { %>
                                                    <% messages.error_msg.forEach(function(msg) { %>
                                                        <% if (msg && msg.length> 0) { %>
                                                            <div class="alert alert-danger alert-dismissible fade show"
                                                                role="alert">
                                                                <div class="alert-content">
                                                                    <i
                                                                        class="fas fa-exclamation-triangle alert-icon"></i>
                                                                    <div class="alert-text">
                                                                        <strong>Error!</strong>
                                                                        <span>
                                                                            <%= msg %>
                                                                        </span>
                                                                    </div>
                                                                </div>
                                                                <button type="button" class="btn-close"
                                                                    data-bs-dismiss="alert"></button>
                                                            </div>
                                                            <% } %>
                                                                <% }); %>
                                                                    <% } %>
                                                                        <% } %>
</div>

<!-- Back to Top Button -->
<button class="back-to-top" id="backToTop" title="Back to Top">
    <i class="fas fa-chevron-up"></i>
</button>

<!-- Floating WhatsApp Button -->
<a href="https://wa.me/919963372123" class="whatsapp-fab" target="_blank" title="Chat with us on WhatsApp">
    <i class="fab fa-whatsapp"></i>
</a>

<style>
    /* Enhanced Navigation Styles */
    .navbar {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-bottom: 1px solid var(--border-light);
        transition: all var(--transition-base);
        box-shadow: var(--shadow-sm);
    }

    .navbar.scrolled {
        padding: 0.75rem 0;
        background: rgba(255, 255, 255, 0.98);
        box-shadow: var(--shadow-md);
    }

    .brand-icon {
        font-size: 2rem;
        margin-right: 0.75rem;
        filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
    }

    .brand-text {
        display: flex;
        flex-direction: column;
        line-height: 1;
    }

    .brand-subtitle {
        font-size: 0.7rem;
        letter-spacing: 2px;
        opacity: 0.8;
        font-weight: 600;
    }

    .brand-title {
        font-size: 1.25rem;
        font-weight: 800;
        color: var(--primary);
    }

    .navbar-brand:hover .brand-icon {
        transform: scale(1.1) rotate(10deg);
    }

    .navbar-brand:hover .brand-title {
        color: var(--accent);
    }

    .nav-link {
        font-weight: 500;
        color: var(--text-body);
        padding: 0.75rem 1rem;
        border-radius: var(--radius-md);
        transition: all var(--transition-base);
        position: relative;
        display: flex;
        align-items: center;
    }

    .nav-link::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        width: 0;
        height: 2px;
        background: var(--accent);
        transition: all var(--transition-base);
        transform: translateX(-50%);
        border-radius: var(--radius-full);
    }

    .nav-link:hover {
        color: var(--accent);
        background: var(--accent-light);
        transform: translateY(-2px);
    }

    .nav-link:hover::before {
        width: 30px;
    }

    .nav-link.active {
        color: var(--accent);
        background: var(--accent-light);
    }

    .nav-link.active::before {
        width: 30px;
    }

    .user-avatar {
        width: 32px;
        height: 32px;
        background: var(--gradient-accent);
        border-radius: var(--radius-full);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        margin-right: 0.75rem;
    }

    .user-name {
        font-weight: 600;
        color: var(--text-main);
    }

    .dropdown-menu {
        border: none;
        box-shadow: var(--shadow-xl);
        border-radius: var(--radius-lg);
        padding: 0.5rem;
        margin-top: 0.5rem;
        animation: fadeInUp 0.3s ease;
    }

    .dropdown-header {
        background: var(--background-alt);
        border-radius: var(--radius-md);
        padding: 1rem;
        margin-bottom: 0.5rem;
        border-bottom: 1px solid var(--border);
    }

    .user-info {
        text-align: center;
    }

    .user-email {
        font-weight: 600;
        color: var(--text-main);
        margin: 0;
        font-size: 0.9rem;
    }

    .user-role {
        color: var(--text-muted);
        margin: 0.25rem 0 0;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .dropdown-item {
        padding: 0.75rem 1rem;
        border-radius: var(--radius-md);
        transition: all var(--transition-base);
        margin: 0.125rem 0;
    }

    .dropdown-item:hover {
        background: var(--accent-light);
        color: var(--accent);
        transform: translateX(4px);
    }

    .dropdown-divider {
        margin: 0.5rem 0;
        border-color: var(--border);
    }

    /* Enhanced Message Styles */
    .message-container {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1050;
        max-width: 400px;
        width: 100%;
    }

    .alert {
        border: none;
        border-radius: var(--radius-lg);
        padding: 1rem 1.5rem;
        margin-bottom: 1rem;
        box-shadow: var(--shadow-lg);
        animation: slideInRight 0.4s ease;
    }

    .alert-success {
        background: var(--accent-light);
        color: var(--primary);
        border-left: 4px solid var(--accent);
    }

    .alert-danger {
        background: #fee2e2;
        color: #991b1b;
        border-left: 4px solid #ef4444;
    }

    .alert-content {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
    }

    .alert-icon {
        font-size: 1.25rem;
        margin-top: 0.125rem;
    }

    .alert-text {
        flex: 1;
    }

    .alert-text strong {
        display: block;
        margin-bottom: 0.25rem;
    }

    .btn-close {
        background: none;
        border: none;
        opacity: 0.7;
        transition: opacity var(--transition-base);
    }

    .btn-close:hover {
        opacity: 1;
    }

    /* WhatsApp FAB */
    .whatsapp-fab {
        position: fixed;
        bottom: 30px;
        right: 30px;
        width: 60px;
        height: 60px;
        background: #25d366;
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 30px;
        box-shadow: 0 10px 25px rgba(37, 211, 102, 0.4);
        z-index: 1000;
        transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        text-decoration: none;
    }

    .whatsapp-fab:hover {
        transform: scale(1.1) rotate(10deg);
        color: white;
        box-shadow: 0 15px 35px rgba(37, 211, 102, 0.5);
    }

    /* Mobile Nav Scroll */
    .nav-scroll-container {
        overflow-x: auto;
        scrollbar-width: none; /* Firefox */
        -ms-overflow-style: none;  /* IE and Edge */
        padding-bottom: 2px;
    }
    
    .nav-scroll-container::-webkit-scrollbar {
        display: none; /* Chrome, Safari and Opera */
    }

    @media (max-width: 768px) {
        .navbar {
            padding: 0.5rem 0 !important;
        }

        .brand-title {
            font-size: 1rem;
        }

        .nav-link {
            padding: 0.4rem 0.6rem;
            font-size: 1rem; /* Make icons big enough to tap */
        }

        .message-container {
            top: 10px;
            right: 10px;
            left: 10px;
            max-width: none;
        }

        .alert {
            padding: 0.75rem 1rem;
        }

        .whatsapp-fab {
            bottom: 20px;
            right: 20px;
            width: 50px;
            height: 50px;
            font-size: 24px;
        }
        
        .dropdown-menu {
            position: absolute !important;
            right: 0;
            left: auto;
            top: 100%;
            width: max-content;
            z-index: 1050;
        }
    }

    /* Scroll Progress Bar */
    .scroll-progress-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: transparent;
        z-index: 2000;
    }

    .scroll-progress-bar {
        height: 100%;
        background: linear-gradient(to right, #1b5e2f, #2d8a4e);
        width: 0%;
        transition: width 0.1s ease;
    }

    /* Back to Top Button */
    .back-to-top {
        position: fixed;
        bottom: 100px;
        right: 30px;
        width: 45px;
        height: 45px;
        background: var(--surface);
        border: 1px solid var(--border);
        color: var(--primary);
        border-radius: 50%;
        display: none;
        align-items: center;
        justify-content: center;
        box-shadow: var(--shadow-lg);
        z-index: 999;
        transition: all 0.3s;
    }

    .back-to-top.show {
        display: flex;
        animation: fadeInUp 0.3s ease;
    }

    .back-to-top:hover {
        background: var(--primary);
        color: white;
        transform: translateY(-5px);
    }

    /* Preloader */
    .preloader {
        position: fixed;
        inset: 0;
        background: #0d2b13;
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: opacity 0.6s ease;
    }

    .preloader.fade-out {
        opacity: 0;
    }

    .loader-content {
        text-align: center;
    }

    .loader-icon {
        font-size: 3rem;
        animation: pulse 1.5s infinite;
        margin-bottom: 1rem;
    }

    .loader-text {
        color: white;
        font-family: 'Playfair Display', serif;
        font-size: 1.5rem;
        letter-spacing: 2px;
        font-weight: 700;
        opacity: 0;
        animation: fadeInUp 0.8s forwards 0.3s;
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); opacity: 1; }
        50% { transform: scale(1.2); opacity: 0.8; }
    }
</style>

<script>
    window.onscroll = function() {
        // Scroll Progress
        const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
        const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        const scrolled = (winScroll / height) * 100;
        document.getElementById("scrollProgress").style.width = scrolled + "%";

        // Back to Top
        const backToTop = document.getElementById("backToTop");
        if (document.body.scrollTop > 500 || document.documentElement.scrollTop > 500) {
            backToTop.classList.add("show");
        } else {
            backToTop.classList.remove("show");
        }
    };

    document.getElementById("backToTop")?.addEventListener("click", () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // Preloader
    window.addEventListener('load', () => {
        const preloader = document.getElementById('preloader');
        preloader.classList.add('fade-out');
        setTimeout(() => {
            preloader.style.display = 'none';
        }, 600);
    });
</script>